# Ultra-Planning V2: System Architecture

> Visual guide to how the compounding intelligence system works

---

## 🏗️ The Three Memory Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                    ULTRA-PLANNING V2 SYSTEM                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│  LAYER 1: VOLATILE MEMORY (Current Session)                     │
├─────────────────────────────────────────────────────────────────┤
│  active/                                                         │
│  ├── task_plan.md          ← Current phases, progress, goals    │
│  └── context.md            ← Research, discoveries this session │
│                                                                  │
│  ledgers/                                                        │
│  └── CONTINUITY_active.md  ← Real-time progress tracking       │
│                                                                  │
│  Duration: Single session                                        │
│  Purpose: Working memory (like RAM)                             │
│  Lifecycle: Created → Updated → Archived                        │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ↓ (extraction)
┌─────────────────────────────────────────────────────────────────┐
│  LAYER 2: PERSISTENT MEMORY (Grows Forever)                     │
├─────────────────────────────────────────────────────────────────┤
│  knowledge/                                                      │
│  ├── patterns.md           ✓ Successful approaches (reuse!)     │
│  ├── failures.md           ⚠️ Known errors (never repeat!)      │
│  ├── decisions.md          🤔 Architecture choices (context)    │
│  ├── gotchas.md            💡 Surprising behaviors (awareness)  │
│  ├── index.md              📇 Search optimization              │
│  ├── code_tldr/            📊 95% compressed code analysis      │
│  └── vectors/              🧠 BGE embeddings for semantic search│
│                                                                  │
│  Duration: Permanent                                             │
│  Purpose: Long-term memory (like disk storage)                  │
│  Lifecycle: Grows exponentially, never deleted                  │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ↓ (reference)
┌─────────────────────────────────────────────────────────────────┐
│  LAYER 3: HISTORICAL ARCHIVE (Read-Only Reference)              │
├─────────────────────────────────────────────────────────────────┤
│  archive/                                                        │
│  └── YYYY-MM-DD_task-name/                                      │
│      ├── task_plan.md       (snapshot of completed work)        │
│      ├── context.md         (what was learned)                  │
│      └── handoff.yaml       (session summary - 90% smaller!)    │
│                                                                  │
│  sessions.db                (cross-terminal session registry)    │
│                                                                  │
│  Duration: Forever (historical)                                  │
│  Purpose: Audit trail, reference                                │
│  Lifecycle: Created when task archived, immutable               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 The Workflow: How Knowledge Flows

### 1. Session Start (Pre-Task Intelligence)

```
┌─────────────────┐
│ User starts     │
│ new task        │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ SessionStart Hook Runs                                       │
├─────────────────────────────────────────────────────────────┤
│ 1. Read knowledge/patterns.md    → Load successful patterns │
│ 2. Read knowledge/failures.md    → Load known errors        │
│ 3. Read knowledge/decisions.md   → Load architecture context│
│ 4. Read handoffs/latest.yaml     → Load previous session    │
│                                                              │
│ Result: Agent starts INFORMED, not from scratch!            │
└─────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ Create Active Session Files                                  │
├─────────────────────────────────────────────────────────────┤
│ • active/task_plan.md (from template)                       │
│ • active/context.md (from template)                         │
│ • ledgers/CONTINUITY_active.md                              │
│                                                              │
│ → Pre-Task Intelligence section auto-filled!                │
└─────────────────────────────────────────────────────────────┘
```

### 2. During Work (Live Updates)

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│ Agent discovers │ ──→ │ Immediately      │ ──→ │ Update          │
│ pattern/error   │     │ document it!     │     │ knowledge/ files│
└─────────────────┘     └──────────────────┘     └─────────────────┘

Example Flow:

User works → Error occurs ──┐
                            │
                            ↓
            PreToolUse Hook shows: "Check failures.md first!"
                            │
                            ↓
          Agent checks failures.md → Error already documented!
                            │
                            ↓
                  Agent avoids error, saves 20 minutes
                            │
                            ↓
          New pattern discovered → Update patterns.md immediately
                            │
                            ↓
          Update task_plan.md: Mark phase complete
                            │
                            ↓
               PostToolUse Hook: "Log error in table"
```

### 3. Session End (Knowledge Extraction)

```
┌─────────────────┐
│ User completes  │
│ or pauses task  │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ SessionEnd Hook Runs                                         │
├─────────────────────────────────────────────────────────────┤
│ Check: Was knowledge/ updated?                               │
│   ✓ Yes: Great! Knowledge captured                          │
│   ✗ No:  Warning! Review and update                         │
└─────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ Manual Archive (./scripts/archive-task.sh)                   │
├─────────────────────────────────────────────────────────────┤
│ 1. Extract learnings: active/context.md → knowledge/*.md    │
│ 2. Create YAML handoff: handoffs/latest.yaml (250 tokens!)  │
│ 3. Move to archive: active/ → archive/YYYY-MM-DD_task/      │
│ 4. Update index: knowledge/index.md statistics              │
│ 5. Clean active/: Ready for next session                    │
└─────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ OR: Daemon Auto-Extraction (idle >5 min)                     │
├─────────────────────────────────────────────────────────────┤
│ Background daemon monitors:                                  │
│   • Last file modification time                              │
│   • If idle >5 min → Auto-extract knowledge                 │
│   • Generate YAML handoff                                    │
│   • Update vectors (if enabled)                              │
│                                                              │
│ Result: Zero manual effort!                                  │
└─────────────────────────────────────────────────────────────┘
```

### 4. Next Session (Compounding Effect)

```
┌─────────────────┐
│ Start session 2 │
└────────┬────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ Agent Loads All Previous Knowledge                           │
├─────────────────────────────────────────────────────────────┤
│ • 1 pattern from session 1 → Reusable!                      │
│ • 1 error documented → Avoid it!                            │
│ • 1 decision made → Context!                                │
│                                                              │
│ Agent IQ: Baseline → +50% smarter                           │
└─────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ Agent Works Faster                                           │
├─────────────────────────────────────────────────────────────┤
│ • Reuses pattern → Saves 30 min                             │
│ • Avoids error → Saves 20 min                               │
│ • References decision → Clear direction                      │
│                                                              │
│ Session 2 time: 60 min vs 90 min (-33%)                     │
└─────────────────────────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────────────────────────┐
│ Adds More Knowledge                                          │
├─────────────────────────────────────────────────────────────┤
│ • 1 new pattern discovered                                   │
│ • 1 new error avoided (different from session 1)            │
│                                                              │
│ Total knowledge: 2 patterns, 2 errors, 1 decision           │
└─────────────────────────────────────────────────────────────┘
```

---

## 🎯 The Compounding Intelligence Loop

```
     SESSION 1                 SESSION 2                 SESSION 3
     ────────                  ────────                  ────────

┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│ Start fresh  │          │ Load 1 KB    │          │ Load 2 KB    │
│ (No prior    │   ──→    │ knowledge    │   ──→    │ knowledge    │
│  knowledge)  │          │              │          │              │
└──────┬───────┘          └──────┬───────┘          └──────┬───────┘
       │                         │                         │
       ↓                         ↓                         ↓
┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│ Work 90 min  │          │ Work 60 min  │          │ Work 45 min  │
│ Discover 1   │          │ Reuse 1      │          │ Reuse 2      │
│  pattern     │          │  pattern     │          │  patterns    │
│ Hit 1 error  │          │ Avoid 1 error│          │ Avoid 2 errors│
└──────┬───────┘          └──────┬───────┘          └──────┬───────┘
       │                         │                         │
       ↓                         ↓                         ↓
┌──────────────┐          ┌──────────────┐          ┌──────────────┐
│ Document:    │          │ Document:    │          │ Document:    │
│ • 1 pattern  │          │ • 1 new      │          │ • 1 new      │
│ • 1 error    │          │   pattern    │          │   pattern    │
│ • 1 decision │          │ • 1 new error│          │              │
└──────┬───────┘          └──────┬───────┘          └──────┬───────┘
       │                         │                         │
       ↓                         ↓                         ↓
  Knowledge: 1 KB          Knowledge: 2 KB          Knowledge: 3 KB
  Agent IQ: 100            Agent IQ: 150            Agent IQ: 220

               ↓ Continue... ↓

     SESSION 10
     ──────────

┌──────────────┐
│ Load 10 KB   │
│ knowledge    │   ← 15 patterns, 12 errors, 8 decisions
└──────┬───────┘
       │
       ↓
┌──────────────┐
│ Work 15 min  │   ← 6x faster than session 1!
│ Reuse 8      │
│  patterns    │
│ Avoid 12     │
│  errors      │
└──────────────┘

  Knowledge: 10 KB
  Agent IQ: 350     ← 3.5x smarter than session 1
```

**This is exponential growth, not linear.**

---

## 🔍 Search Architecture

### Keyword Search (Basic)

```
User query: "authentication"
     │
     ↓
┌────────────────────────────────────────┐
│ grep -i "authentication" knowledge/*.md│
└────────────────────────────────────────┘
     │
     ↓
Results: Exact keyword matches only
  • patterns.md: "JWT Authentication"
  • failures.md: "Authentication timeout"
```

### Semantic Search (Advanced - 90% Better)

```
User query: "how to handle user login"
     │
     ↓
┌─────────────────────────────────────────────────────────┐
│ 1. Encode query with BGE-large model                    │
│    "how to handle user login" → [0.23, -0.45, 0.89...] │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 2. Compare with all knowledge vectors                   │
│    • patterns.md sections: [vector1, vector2, ...]      │
│    • failures.md sections: [vector3, vector4, ...]      │
│    • decisions.md sections: [vector5, vector6, ...]     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
┌─────────────────────────────────────────────────────────┐
│ 3. Calculate cosine similarity                          │
│    • JWT Authentication → 0.92 (very relevant!)         │
│    • OAuth pattern → 0.87 (also relevant!)              │
│    • Database error → 0.23 (not relevant)               │
└────────────────────┬────────────────────────────────────┘
                     │
                     ↓
Results: Semantic matches (no exact keywords needed!)
  • patterns.md#jwt-auth (similarity: 0.92)
  • patterns.md#oauth (similarity: 0.87)
  • failures.md#jwt-verify-error (similarity: 0.81)
```

**Semantic search finds relevant concepts even without exact keyword matches!**

---

## 🌐 Cross-Terminal Architecture

```
┌──────────────────────────────────────────────────────────────┐
│                     LAPTOP (Session 1)                        │
├──────────────────────────────────────────────────────────────┤
│ 1. User works on laptop                                      │
│ 2. session-registry.py register laptop                       │
│    → Writes to sessions.db                                   │
│ 3. Create handoff: handoffs/latest.yaml                      │
│ 4. Knowledge updated: knowledge/*.md                         │
└───────────────────────┬──────────────────────────────────────┘
                        │
                        ↓ (sync via git, Dropbox, or shared drive)
                        │
┌───────────────────────┴──────────────────────────────────────┐
│                     DESKTOP (Session 2)                       │
├──────────────────────────────────────────────────────────────┤
│ 1. User switches to desktop                                  │
│ 2. session-registry.py latest                                │
│    → Reads sessions.db: "laptop was working on auth"         │
│ 3. Load handoff: handoffs/latest.yaml                        │
│    → Full context restored!                                  │
│ 4. Load knowledge: knowledge/*.md                            │
│    → All patterns/errors/decisions available                 │
│ 5. Continue work seamlessly!                                 │
└──────────────────────────────────────────────────────────────┘

Shared State:
  • sessions.db (session registry)
  • handoffs/latest.yaml (session state)
  • knowledge/*.md (permanent memory)
  • archive/ (historical reference)
```

---

## 💾 Token Savings: Before vs After

### Session Handoff Example

**Before (Markdown): 2,500 tokens**
```markdown
# Task: JWT Authentication

## Goal
Implement JWT-based authentication for API endpoints...

## Phases
- [x] Phase 1: Research JWT libraries
  Started: 2026-01-14 14:30
  Completed: 2026-01-14 15:00
  Notes: Evaluated jsonwebtoken, jose, and passport-jwt...

  (continues for 200+ lines)
```

**After (YAML): 250 tokens (90% savings!)**
```yaml
session_id: sess_20260114_laptop
task: jwt_authentication
status: completed
phases:
  - {id: research, status: done, duration_min: 30}
  - {id: implement, status: done, duration_min: 60}
knowledge_created:
  patterns: [{id: jwt_auth, files: ["src/auth/jwt.ts"]}]
  failures: [{id: jwt_verify_error}]
```

### Code Understanding Example

**Before (Raw File): 23,000 tokens**
```typescript
// 1,000 lines of TypeScript code
// Every character costs tokens!
```

**After (TLDR Analysis): 1,330 tokens (95% savings!)**
```json
{
  "L1_AST": {
    "exports": ["generateToken", "verifyToken"],
    "imports": ["jsonwebtoken", "crypto"]
  },
  "L2_CallGraph": {
    "generateToken": {
      "calls": ["jwt.sign"],
      "called_by": ["src/routes/auth.ts"]
    }
  }
}
```

---

## 🎉 The Result

```
Traditional AI Agent:
  • Forgets everything between sessions
  • Repeats same errors
  • No knowledge accumulation
  • Linear growth (or no growth)

Ultra-Planning V2 Agent:
  • ✓ Remembers everything forever
  • ✓ Never repeats documented errors
  • ✓ Knowledge compounds exponentially
  • ✓ 3-5x faster by session 10
  • ✓ 90-95% token savings
  • ✓ Works across all devices
  • ✓ Learns while you're away (daemon)
```

**This is truly compounding intelligence.** 🚀

---

## 📚 Learn More

- **README.md** - Complete documentation
- **QUICKSTART.md** - 5-minute fast track
- **INSTALLATION_COMPLETE.md** - Installation status

**Start building with exponentially smarter AI!**
